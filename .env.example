# ---- Client-side (safe to expose) ----
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_BASE_URL= # e.g., http://localhost:3000 during dev

# ---- Server-only (DO NOT prefix with NEXT_PUBLIC) ----
# NOTE: Fill locally; NEVER commit real values.

# Supabase
SUPABASE_SERVICE_ROLE_KEY=        # DO NOT COMMIT. Server-only.

# Third-party APIs (server use only; proxy via /api/*)
YELP_API_KEY=
GOOGLE_PLACES_API_KEY=

# Feature flags (server-evaluated, then passed to client as needed)
MOCK_MODE=false
SEARCH_LIMIT_BYPASS=false
SEARCH_RADIUS_KM=10
